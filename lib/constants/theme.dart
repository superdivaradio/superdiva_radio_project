import 'package:flutter/material.dart';

class AppTheme {
  // Colores primarios.
  static const headerColor = Color(0xFF0B84AF);
  static const foregroundColor = Color(0xFFFFFFFF);
  static const backgroundColor = Color(0xFFFFFFFF);
  static const buttonPlay = headerColor;
  static const iconSideBar = Colors.white;

  // Personalización detallada.
  static const appBarColor = headerColor;
  static const appBarFontColor = foregroundColor;

  static const metadataColor = Color(0xFF0EBEDD);

  static const controlButtonColor = headerColor;
  static const buttonSplashColor = Color(0xFF17B0D3);
  static const controlButtonIconColor = foregroundColor;

  // Volumen
  static const iconVolume = Color(0xFF17B0D3);
  static const activeColor = Color(0xFF17B0D3);
  static const inactiveColor = Color(0xFFB7C3EC);
  static const thumbColor = Color(0xFF17B0D3);

  // Sombra de la portada del álbum
  static const artworkShadowColor = Color.fromARGB(47, 19, 168, 201);
  static const artworkShadowOffset = Offset(2.0, 2.0);
  static const artworkShadowRadius = 8.0;

  // Colores de redes sociales
  static const socialColor = Color(0xFF17B0D3);

  static var aboutContainerBackgroundColor;

  static var aboutUsFontColor;

  static var aboutUsDescriptionColor;

  static var aboutUsTitleColor;

  static var timerTrackColor;

  static var progressBarColor;

  static var shadowColor;

  static var controlColor;

  static var timerTrackInativeColor;
}